import { Container, NavTop } from "components"
import * as Styled from "./styled"

const Header = () => (
  <Styled.Header>
    <Container>
      <NavTop />
    </Container>
  </Styled.Header>
)

export default Header
